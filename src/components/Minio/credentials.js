/* import * as minio from 'minio'

const credentials = new minio.Client({
    endPoint: "192.168.60.104",
    port: 9000,
    useSSL: false,
    accessKey: "sminio104",
    secretKey: "ssecretminio104"
})

export default credentials */